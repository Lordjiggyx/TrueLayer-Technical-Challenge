//Bringing in express
const express = require("express");
const { Router } = require("express");
//Using express router 
const router = express.Router();


//mkaing thsi file exportble as middleware to be used by express 
module.exports = router;